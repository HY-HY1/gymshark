const express = require('express');
const app = express()

const port = '3002'

app.get() // sets the route up to send info to the client

app.post() // sets the route up to receive a request

app.post('/login', (req, res) => {
    try{

    } catch (error) {

    }
})